{
  "name": "@uiyzzi/pi-asroot",
  "version": "0.2.2",
  "description": "sudo for pi — agent asks, user types their password into a masked TUI input, password never reaches the model",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "tsc && node --test --experimental-strip-types --disable-warning=ExperimentalWarning test/*.test.ts"
  },
  "dependencies": {
    "@uiyzzi/pi-secret-kit": "^0.1.0"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.74.0"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.83.0",
    "@earendil-works/pi-coding-agent": "^0.83.0",
    "@earendil-works/pi-tui": "^0.83.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/uiYzzi/pi_packages.git",
    "directory": "packages/asroot"
  },
  "author": "uiYzzi",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi",
    "sudo",
    "root",
    "askpass",
    "secret"
  ]
}
