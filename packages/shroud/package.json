{
  "name": "@uiyzzi/pi-shroud",
  "version": "0.2.0",
  "description": "High-performance secret firewall for pi — secrets usable as shell vars, never visible to models",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "tsc && node --test --experimental-strip-types --disable-warning=ExperimentalWarning test/*.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.74.0"
  },
  "devDependencies": {
    "@earendil-works/pi-ai": "^0.79.4",
    "@earendil-works/pi-coding-agent": "^0.79.4",
    "@earendil-works/pi-tui": "^0.79.4",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "pi": {
    "extensions": [
      "./dist/index.js"
    ]
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/uiYzzi/pi_packages.git",
    "directory": "packages/shroud"
  },
  "author": "uiYzzi",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi",
    "secret",
    "redact",
    "firewall"
  ]
}
